DB_NAME=policy_rag_tc
DB_USER=postgres
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5432
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b

# Optional - if set, /v1/chat/completions requires "Authorization: Bearer <API_KEY>".
# Leave unset for local/dev use (matches OpenWebUI's OPENAI_API_KEY=not-needed-local).
API_KEY=
WEBUI_SECRET_KEY=change-me-to-a-random-value
