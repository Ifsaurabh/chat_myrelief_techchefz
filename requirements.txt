# Pinned to mutually-compatible versions, verified with
# `pip install --dry-run -r requirements.txt` (no ResolutionImpossible)
# as of this audit (2026-09). Bump together and re-run that check
# rather than editing pins individually.
#
# arize-phoenix and its two openinference packages are NOT in this
# file - they live in requirements-observability.txt and are installed
# in a second step. Reason: crewai pins aiosqlite~=0.21.0, but
# arize-phoenix has required aiosqlite>=0.22.1 since well before the
# Python 3.11 import bug fix (DECISIONS_LOG.md #10) - those ranges
# never overlap, so no single `pip install` can satisfy both. See
# requirements-observability.txt and DECISIONS_LOG.md #14 for details.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.12.4
python-dotenv==1.2.3
psycopg2-binary==2.9.12

docling==2.126.0
llama-index-core==0.14.24
llama-index-vector-stores-postgres==0.9.0
llama-index-embeddings-huggingface==0.8.0
llama-index-llms-ollama==0.11.0

crewai==1.15.20
crewai-tools==1.15.20

ragas==0.4.3
datasets==5.0.1

torch==2.14.0
sentence-transformers==6.0.1
